# Load dataset
# Drop duplicate rows
# Handle missing values (drop rows with missing homeschool_students, fill others)
# Convert 'year' column to integer if possible
# Save cleaned dataset to `outputs/cleaned_data.csv`
